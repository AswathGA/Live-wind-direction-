# Wind Monitor Git Setup Commands

# Navigate to project directory
cd "c:\Users\gillxpc\Desktop\wind"

# Initialize Git repository
git init

# Add all files to staging
git add .

# Create initial commit with descriptive message
git commit -m "Initial commit: Professional wind monitoring system

- Real-time wind data visualization with windrose diagrams
- Flask web server with live API endpoints
- Multi-anemometer support (A, M, P, V, Q, N, S, D, I, T)
- Serial log file monitoring and parsing
- Smooth needle animations and real-time updates
- Professional launcher scripts and EXE creation
- Complete documentation and installation guides"

# Set main branch
git branch -M main

# Add GitHub remote
git remote add origin https://github.com/AswathGA/Live-wind-direction-.git

# Push to GitHub
git push -u origin main
